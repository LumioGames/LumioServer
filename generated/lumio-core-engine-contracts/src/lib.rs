//! Refusal boundary for the unpublished `LumioCoreEngine` Root ABI generated Rust package.

#![forbid(unsafe_code)]

compile_error!(
    "LumioCoreEngine Root ABI generation is blocked on AG-001; refusing to define a local ABI"
);
