# R-00354 live-11 server — implementer report

Status: GREEN (unit/host path). Game 11-scenario harness not run (Game/Architecture not edited).

## RED then GREEN

RED (`dotnet exec` App.Tests `-class LiveElevenPathTests` before production):

- GET `/test-control/bindings` → 404
- TestControl source missing frozen routes
- Admit path discarded binding (`out _`)
- HostTrace had no `netEntityId`
- `LiveElevenHost.cs` missing

GREEN after implementation:

```
dotnet exec mvp-host/tests/Lumio.Server.MvpHost.App.Tests/bin/Debug/net10.0/Lumio.Server.MvpHost.App.Tests.dll
Total: 34, Failed: 0

dotnet exec mvp-host/tests/Lumio.Server.MvpHost.Admission.Tests/bin/Debug/net10.0/Lumio.Server.MvpHost.Admission.Tests.dll
Total: 26, Failed: 0

node .spec/tools/spec-lint.mjs          # OK
node --test .spec/tools/spec-lint.test.mjs  # 13 pass
```

## What landed

- Keep `ConnectionBinding` after Admit; project `nent_*` on 17-key host-audit (`kind:state` + extra keys). No FullSnapshot body `netEntityId`.
- Loopback test-control: bindings/query/chat/tick/expire/snapshot/restore/room-admit.
- C-2 five outcomes (not aliased). C-1 `chat.input` decode then ChatInput text. Tick via `ITimerService`, not a for-loop.
- `ChatRoomWorld` via `Assembly.LoadFrom` (discover origin LumioGame then `wt-game/r-00354-live11` / r-00354).
- ADR-0004 superseded by ADR-0005: C# MVP is not frozen until 11 scenarios are live-green.

## Concerns

- Game launcher must call these routes; this change does not edit Game.
- Discovery currently hits sibling `wt-game/r-00354` dll if live11 Game is unbuilt.
- Architecture `LayeringTest` still flags pre-existing App → `Lumio.Engine.SDK` (unchanged).
