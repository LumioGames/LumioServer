# R-00359 implementer report

Status: **DONE_WITH_CONCERNS**

SHA: `5c097a117c603cab4291d342135550b00cb49b9a`

Branch: `feat/r-00359-suite-2260c85` @ LumioServer `3fcbeef` base.

Target: LumioServer slice-scoped `EntityChatHost` + CoreCLR `ChatRoomWorld` + real Account Server. Not FullGraph. Not `lumio-mvp-host`.

## TDD

1. Current replay (HEAD `3fcbeef`) produced two rounds at `%TEMP%\lumio-r-00359-red-evidence`. Census ids were `"1"`..`"101"`; no `hostProcess`; S8 `entityA` was `"100"`.
2. Game `2260c85` (`2260c859e13d121f75c00f68dedc5e97fc8c80d6`) verifier RED (exit 1). Worktree `r-00354` HEAD is `153b45d` (ahead of origin/main); both verifiers FAIL the same C# MVP predicates.
3. Failing tests added first: `modules/process/tests/verify_rust_evidence.mjs` (9 node tests) and `entity_chat_acceptance.rs` assertions. Cargo acceptance RED: `hostProcess.process must name the rust replay binary, got ""`.
4. Implemented host `nent_{instance}{seq}` projection, rust `hostProcess`, real S5 traces, S8 host NetEntityId rebind, InputCommand on admitted chat, two-round compare of counts / normalized event order / applied Tick.

## RED (Game 2260c85 vs current rust pack)

Command:

```
node C:\Work\LumioGames\wt-game\r-00354\integration\entity-chat\verify-evidence.mjs --dir %TEMP%\lumio-r-00359-red-evidence
```

Also ran the extracted `2260c85` file. Exit 1. Round-1 checks (no secrets):

```
host:mvp
host:audit
census:bots
census:player
census:total
scenario-5:suite-double
scenario-7:suite-double
scenario-8
scenario-9:suite-double
scenario-10:suite-double
scenario-11:suite-double
s4:binding
s3:playwright
s6:timer
s7:snapshot-material
s8:rebind
```

`hasMvpHostProcessAudit` lock: Game `verify-evidence.mjs` lines **243–246** (`process === 'lumio-mvp-host'`).

## GREEN (this repo)

```
node --test modules/process/tests/verify_rust_evidence.mjs
# 9 pass / 0 fail

cargo test -p lumio-server-process --test entity_chat_acceptance --locked
# 1 passed; 0 failed; ~16s

cargo fmt --all -- --check
cargo clippy -p lumio-server-process --all-targets --all-features --locked -- -D warnings
node .spec/tools/spec-lint.mjs          # spec-lint: OK
node --test .spec/tools/spec-lint.test.mjs  # 13 pass
```

Rust oracle against GREEN pack: `%TEMP%\lumio-r-00359-entity-chat-evidence` → `ok: true`.

Evidence (no secrets): `hostProcess.process = lumio-entity-chat-replay` + live pid; 101 `nent_*` ids in `host-audit.ndjson`; S8 same host NetEntityId, `sess-Bot100` → `sess-Bot100-re`; S5 traces unauthorized/invisible/stale; S6 InputCommand `chat.input` + sha256; S9 A/B distinct nent; two-round event-order keys and appliedTicks match.

## Game verifier after GREEN (still FAIL, expected)

Same command on `%TEMP%\lumio-r-00359-entity-chat-evidence`. Exit 1. Round-1 checks:

```
host:mvp
host:audit
census:bots / census:player / census:total   # empty because !hasMvpHostProcessAudit
scenario-5/7/9/10/11:suite-double            # rust really executed ok:true
s4:binding                                   # follows empty census
s3:playwright                                # in-process host, ran:false
s6:timer                                     # tick-batched; no Client Timer Manager
s7:suite-double
event-order-compare                          # nent instance prefix differs per process
```

**S8 rebind is no longer a Game failure** (host `nent_*` is honest). Remaining Game FAIL is the C# MVP SUCCESS predicate, not missing rust work.

Did **not** set `hostProcess.process = "lumio-mvp-host"`.

## Known gaps

- Playwright Chromium: in-process `EntityChatHost` has no listen URI / page. `playwright.ran = false`, `injected = false`. S3 admission (1 PlayerEntity, total 101) is real.
- S6 Client Timer Manager not invoked (`timerManagerInvoked: false`, cadence `tick-batched`). Chat is admitted via C-1 InputCommand then `run_tick`.
- Game `2260c85` oracle is host-agnostic-contract + C# MVP lock. Dispatcher follow-up on Game; do not fork contracts here.
- FullGraph cap is 128 after Server PR #18; this slice does not clone FullGraph.

## Change list

- `modules/process/src/entity_chat/host.rs` — `nent_*` host NetEntityId, sessionId, `list_admits`
- `modules/process/src/entity_chat/suite.rs` — rust evidence pack
- `modules/process/tests/verify_rust_evidence.mjs` — rust SUCCESS oracle
- `modules/process/tests/entity_chat_acceptance.rs` — two-round + oracle
- `.spec/knowledge/features/rust-entity-chat-host.md`, `.spec/decisions/0004-csharp-mvp-host-frozen-reference.md`

## Knowledge

Updated rust-entity-chat-host + ADR 0004. No new public contract.
